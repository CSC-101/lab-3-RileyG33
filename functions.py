# Double the value of a number.
# Input: a number to be doubled
# Result: a number
def double(n:int) -> int:
    result = n * 2
    return result
#No, passing test_double_one would not mean the code is correct.
#Single passing test only confirms the code works for one specfic scenario and doesn't
#account for all logic paths.